// enum to represent packet type
// traditional enum 
// these values represent the various packet types that will be encounterd in a network:

enum PacketType{    // 1.
    UNKNOWN,
    DATA,
    CONTROL,
    GROUP,
    RESOLUTION,
    ERROR
};
